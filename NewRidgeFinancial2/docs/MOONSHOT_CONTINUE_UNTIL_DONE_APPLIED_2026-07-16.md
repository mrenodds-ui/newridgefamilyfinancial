# Continue until all done — APPLIED (real NR2 paths)

**Date:** 2026-07-16  
**Consult:** `MOONSHOT_WHATS_NEXT_AFTER_OM_SCHEDULE_TRACK_2026-07-16.md`  
**Operator:** continue until all are done

Moonshot invented `nr2/services/push_engagement.py` etc. — **not built**. Mapped to real files only.

## Ordered completion

| # | Intent | Real ship |
|---|--------|-----------|
| 1 | PushEngage flash-risk track hygiene | Commit consult + APPLIED; HAL doctrine: avoid third-party chat/push embeds |
| 2 | Morning-bundle visibility | `period_close_status.morningBundle` from last close `export` |
| 3 | Force Close shared wire | Hub + OM use `NR2OpticalWire.bindForceCloseButton` |
| 4 | Optical honesty polish | HAL uses wire `honestyMoney`; Claims snap never paints `$0` |

## Explicitly not done (by design)

- **Do not** flip `forceCloseAvailable` to always-true — remains laser/stall gated.
- **Do not** build PushEngage runtime / flashRiskEligible fake services.
- **Do not** redo Trellis huddle / OM schedule enrich.

## Validation

- Desk smoke: `thisPatientShortcutCovered`, `monThuApptTimeOk` remain green when lasers match gate.
- Unit: portal ops / patient dossier / desk smoke suites.
